// Package serverupdate implements MCP tools for checking and applying updates
// to this MCP server binary.
//
// The package uses the autoupdate subsystem and GitHub release metadata rather
// than a GitLab API endpoint.
package serverupdate
