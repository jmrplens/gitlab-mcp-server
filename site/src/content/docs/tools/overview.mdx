---
title: Tools Overview
description: How GitLab MCP Server's three tool modes — dynamic, meta-tool, and individual — expose 1,000+ GitLab operations while trading token cost for granularity.
chips:
  - text: Catalog-first
  - text: Three surfaces
    href: /gitlab-mcp-server/tools/dynamic-tools/
  - text: Typed schemas
datePublished: "2026-04-21"
head:
  # The page an AI reads to answer "what can this server do?". The prose groups
  # the base meta-tools into eight families, but nothing in the markup said so —
  # each family now has a citable deep link.
  - tag: script
    attrs:
      type: application/ld+json
    content: |
      {
        "@context": "https://schema.org",
        "@type": "ItemList",
        "@id": "https://jmrplens.github.io/gitlab-mcp-server/tools/overview/#meta-tool-families",
        "name": "GitLab MCP Server base meta-tool families",
        "description": "The eight families the base meta-tools are grouped into in meta-tool mode. Each family covers a set of GitLab domains reachable through a single domain dispatcher tool.",
        "itemListOrder": "https://schema.org/ItemListOrderAscending",
        "numberOfItems": 8,
        "isPartOf": { "@id": "https://jmrplens.github.io/gitlab-mcp-server/tools/overview/#article" },
        "about": { "@id": "https://github.com/jmrplens/gitlab-mcp-server#software" },
        "itemListElement": [
          {
            "@type": "ListItem",
            "position": 1,
            "name": "Project management",
            "description": "Projects, issues, groups, users and wikis: gitlab_project, gitlab_issue, gitlab_group, gitlab_user, gitlab_wiki.",
            "url": "https://jmrplens.github.io/gitlab-mcp-server/tools/overview/#project-management"
          },
          {
            "@type": "ListItem",
            "position": 2,
            "name": "Code & repository",
            "description": "Branches, tags, releases, repository trees and merge requests: gitlab_branch, gitlab_tag, gitlab_release, gitlab_repository, gitlab_merge_request.",
            "url": "https://jmrplens.github.io/gitlab-mcp-server/tools/overview/#code--repository"
          },
          {
            "@type": "ListItem",
            "position": 3,
            "name": "Code review",
            "description": "Merge request review surface — approvals, discussions, draft notes and diffs: gitlab_mr_review.",
            "url": "https://jmrplens.github.io/gitlab-mcp-server/tools/overview/#code-review"
          },
          {
            "@type": "ListItem",
            "position": 4,
            "name": "CI/CD",
            "description": "Pipelines, jobs, runners, CI variables and environments: gitlab_pipeline, gitlab_job, gitlab_runner, gitlab_ci_variable, gitlab_environment.",
            "url": "https://jmrplens.github.io/gitlab-mcp-server/tools/overview/#cicd"
          },
          {
            "@type": "ListItem",
            "position": 5,
            "name": "Search & analysis",
            "description": "Instance, group and project scoped search across code, issues, merge requests, commits and more: gitlab_search.",
            "url": "https://jmrplens.github.io/gitlab-mcp-server/tools/overview/#search--analysis"
          },
          {
            "@type": "ListItem",
            "position": 6,
            "name": "Access & credentials",
            "description": "Access tokens, deploy keys, deploy tokens and instance administration: gitlab_access, gitlab_admin.",
            "url": "https://jmrplens.github.io/gitlab-mcp-server/tools/overview/#access--credentials"
          },
          {
            "@type": "ListItem",
            "position": 7,
            "name": "Packages & content",
            "description": "Package registry, snippets and project templates: gitlab_package, gitlab_snippet, gitlab_template.",
            "url": "https://jmrplens.github.io/gitlab-mcp-server/tools/overview/#packages--content"
          },
          {
            "@type": "ListItem",
            "position": 8,
            "name": "Discovery & utilities",
            "description": "Feature flags, model registry, CI/CD catalog, custom emoji and repository storage moves, plus the standalone project-resolution and interactive creation tools: gitlab_feature_flags, gitlab_model_registry, gitlab_ci_catalog, gitlab_custom_emoji, gitlab_storage_move, gitlab_discover_project, gitlab_interactive_*.",
            "url": "https://jmrplens.github.io/gitlab-mcp-server/tools/overview/#discovery--utilities"
          }
        ]
      }
  # The tool catalog is also published as two machine-readable files. Describing
  # them as a Dataset gives a crawler a reason to fetch the full catalog instead
  # of inferring it from prose.
  - tag: script
    attrs:
      type: application/ld+json
    content: |
      {
        "@context": "https://schema.org",
        "@type": "Dataset",
        "@id": "https://github.com/jmrplens/gitlab-mcp-server#tool-catalog",
        "translationOfWork": { "@id": "https://github.com/jmrplens/gitlab-mcp-server#tool-catalog-es" },
        "isPartOf": { "@id": "https://jmrplens.github.io/gitlab-mcp-server/tools/overview/#article" },
        "name": "GitLab MCP Server tool catalog",
        "description": "The complete machine-readable catalog of GitLab MCP Server's MCP surface: every tool and action name with its description and parameters, plus the resources, prompts and meta-tool groups, generated from the canonical action catalog at build time.",
        "inLanguage": "en",
        "license": "https://opensource.org/licenses/MIT",
        "isAccessibleForFree": true,
        "creator": { "@id": "https://jmrp.io/#person" },
        "about": { "@id": "https://github.com/jmrplens/gitlab-mcp-server#software" },
        "keywords": "Model Context Protocol, MCP tools, GitLab API, tool catalog, llms.txt",
        "distribution": [
          {
            "@type": "DataDownload",
            "name": "llms.txt — catalog summary",
            "encodingFormat": "text/markdown",
            "contentUrl": "https://jmrplens.github.io/gitlab-mcp-server/llms.txt"
          },
          {
            "@type": "DataDownload",
            "name": "llms-full.txt — full tool and action reference",
            "encodingFormat": "text/markdown",
            "contentUrl": "https://jmrplens.github.io/gitlab-mcp-server/llms-full.txt"
          }
        ]
      }
faq:
  - q: "What is the difference between meta-tool mode and dynamic mode?"
    a: "Both meta-tool and dynamic mode reduce token cost compared with individual mode, but they package operations differently. **Meta-tool mode** exposes a fixed list of {{meta.base}} domain-level tools, each routing on an `action` parameter — useful when a client works best with a stable, browsable tool list. **Dynamic mode** exposes only two tools and resolves operations on demand through search, giving the smallest possible tool list. Both share the same handlers and safety behavior."
  - q: "Do all modes support the same GitLab operations?"
    a: "Yes. Dynamic, meta-tool, and individual modes are all projected from one canonical action catalog, so they cover the same GitLab REST and GraphQL operations. They differ only in packaging, not capability. Read-only filtering, safe-mode previews, destructive-action confirmations, token-scope filtering, and result formatting behave identically regardless of the mode you choose."
  - q: "How many tools does each mode expose?"
    a: "Individual mode exposes {{tools.free}} tools on CE, {{tools.ultimate_self_managed}} on self-managed Enterprise/Premium, and {{tools.gitlab_com}} on GitLab.com when Orbit is available. Meta-tool mode exposes {{meta.base}} base domain tools (more with the Enterprise catalog). Dynamic mode always exposes exactly two public tools, regardless of how many GitLab operations are available."
  - q: "Do Enterprise tools require a paid GitLab license?"
    a: "Yes. Enterprise meta-tools — such as merge trains, DORA metrics, vulnerabilities, and compliance policies — require a GitLab Premium or Ultimate license on the connected instance. Forcing the Enterprise catalog on a Community Edition instance registers the tools, but the underlying API calls return permission errors because the features are not licensed."
  - q: "What are the three tool modes in GitLab MCP Server?"
    a: "GitLab MCP Server offers three tool modes that expose the same GitLab API surface with different packaging. Dynamic mode (the default) exposes two public tools that search and execute a canonical action catalog. Meta-tool mode groups related operations into domain-level tools that route on an action parameter. Individual mode registers one MCP tool per GitLab operation. All three modes share identical handlers, schemas, and safety behavior."
  - q: "Which tool mode is the most token-efficient?"
    a: "Dynamic mode is the most token-efficient because it exposes only two public tools regardless of how many GitLab operations exist. The assistant first calls gitlab_find_action to retrieve an action and its schema, then calls gitlab_execute_action. This keeps the tool list small in the model's context window while still reaching every GitLab operation through the shared action catalog."
---

import stats from "../../../data/stats.json";
import Facts from "../../../components/docs/Facts.astro";
import Fact from "../../../components/docs/Fact.astro";
import FAQ from "../../../components/FAQ.astro";

GitLab MCP Server exposes GitLab operations as MCP tools that AI assistants can invoke directly. It offers **three tool modes**: dynamic, meta-tool, and individual. All three reach the same GitLab API surface. They differ only in packaging, so the choice trades context cost against tool-list granularity — never capability.

Every mode is projected from one **canonical action catalog**. That gives all three identical handlers, typed schemas, and safety behavior. Only the presentation changes. Dynamic mode searches and executes canonical `domain.action` IDs. Meta-tool mode groups actions by domain. Individual mode registers one tool per operation.

## Which tool mode should I use?

**Dynamic mode is the default, and the right choice for most assistants.** It keeps two tools in the model's context and still reaches every GitLab operation. Choose meta-tool mode when your client works better with a fixed, domain-level tool list. Choose individual mode only when a client needs every operation enumerated up front.

| Mode                    | Public tools exposed                                       | Best for                                      | Relative token cost |
| ----------------------- | ---------------------------------------------------------- | --------------------------------------------- | ------------------- |
| **Dynamic** _(default)_ | 2 — `gitlab_find_action`, `gitlab_execute_action`          | Most assistants; lowest context overhead      | Lowest              |
| **Meta-tool**           | {stats.meta.base} base domain tools (more with Enterprise) | A fixed, discoverable, domain-level tool list | Medium              |
| **Individual**          | {stats.tools.free}–{stats.tools.gitlab_com} tools          | Clients that need every operation pre-listed  | Highest             |

Set the mode with the `TOOL_SURFACE` environment variable (`dynamic`, `meta`, or `individual`). Dynamic mode is used when `TOOL_SURFACE` is unset.

## Operating modes

### How does individual mode work?

Individual mode registers **one MCP tool per GitLab operation**: **{stats.tools.free} tools** for CE, **{stats.tools.ultimate_self_managed} tools** for self-managed Enterprise/Premium, or **{stats.tools.gitlab_com} tools** on GitLab.com Enterprise/Premium when Orbit is available. This gives an AI assistant maximum granularity — every operation is a distinct, individually described tool — at the cost of significant context tokens spent listing and disambiguating tools during discovery.

<Facts>
	<Fact kind="exposes">
		one visible MCP tool per GitLab operation, each with its own typed input
		and output schema — the whole catalog, flat.
	</Fact>
	<Fact kind="requires">`TOOL_SURFACE=individual` (or legacy `META_TOOLS=false`), and a client with the context budget to hold the full tool list.</Fact>
	<Fact kind="tier">
		the tool count itself moves with the licence: Free/CE registers the base
		catalog, Premium and Ultimate add their gated tools, and lower tiers
		never see higher-tier input fields in the schemas.
	</Fact>
	<Fact kind="notCovered">
		low-token startup: listing every tool is the point of this surface, so
		the discovery cost cannot be reduced without changing surface. Clients
		with small context windows should use the dynamic default instead.
	</Fact>
</Facts>

### How does meta-tool mode work?

Meta-tool mode (`TOOL_SURFACE=meta`) consolidates related operations into **domain-level meta-tools**. Each meta-tool accepts an `action` parameter that routes to the appropriate handler, reducing the tool count to **{stats.meta.base} base meta-tools**, **{stats.meta.self_managed_enterprise} on self-managed Enterprise/Premium**, or **{stats.meta.gitlab_com} on GitLab.com Enterprise/Premium** when Orbit is available. This dramatically improves token efficiency over individual mode while keeping a fixed, browsable tool list.

```json
{
  "tool": "gitlab_issue",
  "arguments": {
    "action": "create",
    "project": "my-group/my-project",
    "title": "Fix login redirect",
    "description": "Users are redirected to 404 after login",
    "labels": "bug,priority::high"
  }
}
```

<Facts>
	<Fact kind="exposes">
		one dispatcher tool per domain (projects, issues, merge requests,
		pipelines…), each routing an `action` parameter to the same handlers the
		other surfaces use.
	</Fact>
	<Fact kind="requires">`TOOL_SURFACE=meta`. Per-action call shapes stay discoverable through `gitlab://tools` and `gitlab://tools/{id}` whatever `META_PARAM_SCHEMA` is set to.</Fact>
	<Fact kind="tier">
		Premium/Ultimate add dedicated enterprise meta-tools and inject
		enterprise-only routes into `gitlab_project`, `gitlab_group` and
		`gitlab_issue`.
	</Fact>
	<Fact kind="notCovered">
		per-operation tool descriptions in the client's tool list: a dispatcher
		summarises its domain, so an assistant that needs an exact schema reads
		it from the catalog resources rather than from `tools/list`.
	</Fact>
</Facts>

### How does the dynamic toolset work?

Dynamic mode (`TOOL_SURFACE=dynamic`) is the low-token default. It exposes only two public tools — `gitlab_find_action` and `gitlab_execute_action` (the singular `gitlab_execute_action` name is intentional) — while keeping every GitLab action reachable through the canonical catalog. The assistant first finds an action and its exact schema, then executes the canonical `domain.action` ID.

```mermaid
flowchart LR
	A[Find action and schema] --> B[Execute domain.action]
	B --> C[Existing GitLab handler]
```

Because all three modes share the canonical action catalog, **safety behavior stays consistent across every surface**: read-only filtering, safe-mode previews, token-scope filtering, destructive-action confirmations, schemas, and result formatting are identical.

<Facts>
	<Fact kind="exposes">
		two tools — `gitlab_find_action` and `gitlab_execute_action` — reaching
		the full canonical catalog: find returns exact schemas, execute runs the
		canonical `domain.action`.
	</Fact>
	<Fact kind="requires">nothing: it is the default when `TOOL_SURFACE` is unset. `CAPABILITY_SURFACE=minimal` shrinks the shared resource/prompt footprint further.</Fact>
	<Fact kind="tier">
		invisible in `tools/list` (always two tools) but real in reach: find
		only surfaces, and execute only accepts, the actions the resolved tier
		registers.
	</Fact>
	<Fact kind="notCovered">
		a browsable tool list: the client sees two tools, so a human scanning
		`tools/list` learns nothing about coverage — that is what
		`gitlab://tools` and the find tool are for.
	</Fact>
</Facts>

## Tool naming convention

All tools follow a consistent naming pattern:

- **Individual tools**: `gitlab_{domain}_{action}` (e.g., `gitlab_issue_create`, `gitlab_project_list`) — domain first, not verb first
- **Meta-tools**: `gitlab_{domain}` (e.g., `gitlab_issue`, `gitlab_project`)
- **Dynamic actions**: canonical `domain.action` IDs executed through `gitlab_execute_action` (e.g., `issue.create`, `merge_request.list`)

## Base meta-tools ({stats.meta.base})

### Project management

| Meta-tool        | Description                                                                                                                          | Key Actions                                                                                                  |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------ |
| `gitlab_project` | Project CRUD, configuration, hooks, labels, milestones, members, badges, boards, integrations, Pages, and uploads                    | list, get, create, update, delete, fork, star, archive, label\_\*, milestone\_\*, members, badge\_\*, upload |
| `gitlab_issue`   | Issue lifecycle, notes, discussions, links, work items (including work item types), time tracking, award emoji, and resource events  | list, get, create, update, delete, note\_\*, link\_\*, discussion\_\*, work_item\_\*, time\_\*               |
| `gitlab_group`   | Group CRUD, subgroups, members, badges, hooks, labels, milestones, transfer, and descendant projects                                 | list, get, create, update, delete, group_label\_\*, group_milestone\_\*, group_member\_\*, badge\_\*         |
| `gitlab_user`    | User information, status, SSH keys, GPG keys, emails, activity, preferences, to-do items, and instance service accounts (Enterprise) | get, current, list, ssh_key\_\*, gpg_key\_\*, email\_\*, status\_\*, todo\_\*, service_account\_\*           |
| `gitlab_wiki`    | Wiki page management and attachments                                                                                                 | list, get, create, update, delete, upload_attachment                                                         |

### Code & repository

| Meta-tool              | Description                                                                                                                                         | Key Actions                                                                                   |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| `gitlab_branch`        | Branch management and protection rules (including branch rule queries via GraphQL)                                                                  | list, get, create, delete, protect, unprotect, branch_rule\_\*                                |
| `gitlab_tag`           | Tag management and protection rules with GPG signature verification                                                                                 | list, get, create, delete, protect, unprotect                                                 |
| `gitlab_release`       | Release management and release asset links                                                                                                          | list, get, create, update, delete, link\_\*                                                   |
| `gitlab_repository`    | Repository tree, files, commits, diffs, blame, compare (including cross-project), cherry-pick, revert, contributors, archives, changelogs, markdown | tree, file\_\*, commit\_\*, compare, blame, archive, changelog, submodule\_\*, discussion\_\* |
| `gitlab_merge_request` | MR lifecycle, approvals, approval rules, time tracking, subscriptions, context commits, award emoji, and resource events                            | list, get, create, update, merge, rebase, approve, approval\_\*, time\_\*, event\_\*          |

### Code review

| Meta-tool          | Description                                                                | Key Actions                                                |
| ------------------ | -------------------------------------------------------------------------- | ---------------------------------------------------------- |
| `gitlab_mr_review` | MR notes, threaded discussions, code diffs, draft notes, and diff versions | note\_\*, discussion\_\*, diff\_\*, draft\_\*, version\_\* |

### CI/CD

| Meta-tool            | Description                                                                                                  | Key Actions                                                               |
| -------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------- |
| `gitlab_pipeline`    | Pipeline management, resource groups, test reports, trigger tokens, pipeline bridges, and pipeline schedules | list, get, create, cancel, retry, delete, wait, schedule\_\*, trigger\_\* |
| `gitlab_job`         | CI job management, artifacts, logs, and Kubernetes agents (force cancel supported)                           | list, get, play, cancel, retry, erase, trace, artifacts, wait, k8s\_\*    |
| `gitlab_runner`      | CI/CD runner management, runner controllers, controller scopes, and controller tokens                        | list, get, update, remove, jobs, controller\_\*, register, verify         |
| `gitlab_ci_variable` | CI/CD variables at instance, group, and project scope                                                        | list, get, create, update, delete (at each scope level)                   |
| `gitlab_environment` | Environment management, protected environments, deployment freeze periods, and deployment records            | list, get, create, update, delete, stop, deployment\_\*, freeze\_\*       |

### Search & analysis

| Meta-tool       | Description                                                     | Key Actions                                                                               |
| --------------- | --------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| `gitlab_search` | Cross-resource search across projects, groups, and global scope | code, issues, merge_requests, commits, milestones, notes, projects, snippets, users, wiki |

### Access & credentials

| Meta-tool       | Description                                                                                    | Key Actions                                                             |
| --------------- | ---------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `gitlab_access` | Deploy keys, deploy tokens, project access tokens, group access tokens, and access requests    | deploy_key\_\*, deploy_token\_\*, project_token\_\*, group_token\_\*    |
| `gitlab_admin`  | Instance administration: Sidekiq, settings, license, broadcast messages, hooks, PATs, and more | sidekiq\_\*, setting\_\*, license\_\*, broadcast\_\*, hook\_\*, pat\_\* |

### Packages & content

| Meta-tool         | Description                                                                   | Key Actions                                                 |
| ----------------- | ----------------------------------------------------------------------------- | ----------------------------------------------------------- |
| `gitlab_package`  | Package registry management and generic package file upload/download          | list, get, delete, upload, download, protection_rule\_\*    |
| `gitlab_snippet`  | Project and personal snippets with discussions, notes, and award emoji        | list, get, create, update, delete, discussion\_\*, note\_\* |
| `gitlab_template` | Project templates (gitignores, CI YAML, Dockerfiles, licenses) and CI linting | gitignore\_\*, ci\_\*, dockerfile\_\*, license\_\*, lint    |

### Discovery & utilities

| Meta-tool               | Description                                                         | Key Actions                                      |
| ----------------------- | ------------------------------------------------------------------- | ------------------------------------------------ |
| `gitlab_feature_flags`  | Feature flag and feature flag user list management                  | list, get, create, update, delete, user_list\_\* |
| `gitlab_model_registry` | ML model package file download from GitLab Model Registry           | download                                         |
| `gitlab_ci_catalog`     | CI/CD Catalog resource discovery (reusable components, templates)   | list, get                                        |
| `gitlab_custom_emoji`   | Group-level custom emoji management via GraphQL                     | list, get, create, delete                        |
| `gitlab_storage_move`   | Repository storage moves for projects, snippets, and groups (admin) | retrieve\_\*, get\_\*, schedule\_\*              |

Five more tools are registered alongside the dispatchers. They take their parameters directly, with no `action` envelope, and keep the same names in individual mode:

| Tool                                | Description                                                       |
| ----------------------------------- | ----------------------------------------------------------------- |
| `gitlab_discover_project`           | Resolve a full git remote URL to a GitLab project ID and metadata |
| `gitlab_interactive_issue_create`   | Guided issue creation through MCP elicitation                     |
| `gitlab_interactive_mr_create`      | Guided merge request creation through MCP elicitation             |
| `gitlab_interactive_project_create` | Guided project creation through MCP elicitation                   |
| `gitlab_interactive_release_create` | Guided release creation through MCP elicitation                   |

:::note
A `gitlab_server` dispatcher is registered alongside these and is not counted in the {stats.meta.base} figure above. It is read-only, with two actions: `status` and `health_check`. Individual mode registers `gitlab_server_status`; dynamic mode reaches both through the canonical action catalog. There is no self-update action to call, because the binary belongs to whichever channel installed it.
:::

## Enterprise-only tools

When the Enterprise/Premium catalog is enabled, the server registers {stats.meta.self_managed_enterprise - stats.meta.base} additional self-managed meta-tools for GitLab Premium and Ultimate features:

`gitlab_merge_train`, `gitlab_audit_event`, `gitlab_dora_metrics`, `gitlab_dependency`, `gitlab_external_status_check`, `gitlab_group_scim`, `gitlab_member_role`, `gitlab_enterprise_user`, `gitlab_attestation`, `gitlab_compliance_policy`, `gitlab_project_alias`, `gitlab_geo`, `gitlab_vulnerability`, `gitlab_security_attribute`, `gitlab_security_category`, `gitlab_security_finding`, `gitlab_security_scan_profile`

On GitLab.com Enterprise/Premium, the catalog also registers `gitlab_orbit` with six read-only Knowledge Graph actions: `status`, `schema`, `tools`, `dsl`, `query`, and `graph_status`.

:::note
Enterprise tools require a GitLab Premium or Ultimate license on your instance. Forcing the Enterprise/Premium catalog on a Community Edition instance will register the tools but API calls will return permission errors.
:::

<FAQ />

## Further reading

- [Meta-tools](/gitlab-mcp-server/tools/meta-tools/) — detailed meta-tool architecture and usage
- [Dynamic toolset](/gitlab-mcp-server/tools/dynamic-tools/) — low-token search/describe/execute mode
- [Orbit](/gitlab-mcp-server/tools/orbit/) — GitLab.com Enterprise/Premium Knowledge Graph tools
- [Resources & Prompts](/gitlab-mcp-server/tools/resources-prompts/) — read-only context and prompt templates

## External references

- [Model Context Protocol specification](https://modelcontextprotocol.io/) — the protocol these tools implement
- [GitLab REST API v4](https://docs.gitlab.com/api/rest/) — the REST surface most tools call
- [GitLab GraphQL API](https://docs.gitlab.com/api/graphql/) — the GraphQL surface used by the remaining domains

## Source reference

:::note[Developer Documentation]
This page is the user-facing overview. For the full per-domain tool reference, see [docs/reference/tools](https://github.com/jmrplens/gitlab-mcp-server/blob/main/docs/reference/tools/README.md).
:::
