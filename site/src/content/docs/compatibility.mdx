---
title: Compatibility
description: Platform, GitLab edition, and MCP client compatibility for GitLab MCP Server — CE and EE, six OS/arch binaries, and any MCP stdio or HTTP client.
head:
  - tag: script
    attrs:
      type: application/ld+json
    content: |
      {
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "mainEntity": [
          {
            "@type": "Question",
            "name": "Does GitLab MCP Server work with GitLab Community Edition?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Yes. GitLab MCP Server works with both Community Edition (CE) and Enterprise Edition (EE). On CE it exposes the full base catalog — base meta-tools, MCP resources, and prompts — covering projects, issues, merge requests, pipelines, CI/CD, wikis, releases, users, groups, search, deployments, environments, and packages. Enterprise-only features such as merge trains, DORA metrics, vulnerability management, audit events, and compliance policies require a Premium or Ultimate license and are not available on CE."
            }
          },
          {
            "@type": "Question",
            "name": "How do I enable Enterprise tools in GitLab MCP Server?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Set the licensing tier explicitly or let the server auto-detect it. In stdio mode, set GITLAB_TIER=premium or GITLAB_TIER=ultimate; in HTTP mode pass --tier=premium or --tier=ultimate. When the tier is omitted, the server auto-detects the edition from GET /license per token and URL entry, falling back to free. Forcing the Enterprise/Premium catalog adds 16 enterprise-only meta-tools. The legacy GITLAB_ENTERPRISE=true (stdio) and --enterprise (HTTP) flags are still honored for back-compat but are deprecated."
            }
          },
          {
            "@type": "Question",
            "name": "Which operating systems and architectures does GitLab MCP Server support?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Pre-built binaries are available for Linux, macOS, and Windows on both amd64 and arm64 — six binaries in total. macOS ships separate Intel (amd64) and Apple Silicon (arm64) builds. Each platform is a single static binary with no runtime dependencies."
            }
          },
          {
            "@type": "Question",
            "name": "Which MCP clients are compatible with GitLab MCP Server?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Any client that supports the Model Context Protocol can use GitLab MCP Server. Tested stdio clients include VS Code with GitHub Copilot, Claude Desktop, Cursor, Claude Code (CLI), Windsurf, JetBrains IDEs, Zed, and Kiro. Any Streamable HTTP client can connect through HTTP mode. The server also supports MCP resources, prompts, completions, elicitation, and progress notifications."
            }
          }
        ]
      }
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import stats from "../../data/stats.json";

:::note[Developer Documentation]
This page is the user-facing compatibility matrix. For tier flags, environment variables, and per-client configuration, see [docs/reference/configuration](https://github.com/jmrplens/gitlab-mcp-server/blob/main/docs/reference/configuration.md).
:::

## GitLab edition compatibility

GitLab MCP Server works with both Community Edition (CE) and Enterprise Edition (EE). The Enterprise/Premium catalog unlocks 16 additional enterprise-only meta-tools: set `GITLAB_TIER=premium` (or `GITLAB_TIER=ultimate`) in stdio mode, use `--tier=premium`/`--tier=ultimate` in HTTP mode, or rely on auto-detection from `GET /license` (fallback `free`). The legacy boolean `GITLAB_ENTERPRISE=true` (stdio) and `--enterprise` (HTTP) flags are still honored for back-compat but are deprecated.

| Feature                                              | Community (CE) | Enterprise (EE) |
| ---------------------------------------------------- | :------------: | :-------------: |
| Projects, Issues, MRs, Pipelines, CI/CD              |       ✅       |       ✅        |
| Wikis, Labels, Milestones, Releases                  |       ✅       |       ✅        |
| Users, Groups, Members, Search                       |       ✅       |       ✅        |
| Deployments, Environments, Packages                  |       ✅       |       ✅        |
| {stats.meta.base} base meta-tools                    |       ✅       |       ✅        |
| {stats.resources} resources, {stats.prompts} prompts |       ✅       |       ✅        |
| Merge Trains                                         |       ❌       |       ✅        |
| DORA Metrics                                         |       ❌       |       ✅        |
| Vulnerability Management                             |       ❌       |       ✅        |
| Audit Events                                         |       ❌       |       ✅        |
| Compliance Policies                                  |       ❌       |       ✅        |
| +16 enterprise meta-tools                            |       ❌       |       ✅        |

To enable enterprise features in stdio mode, set `GITLAB_TIER=premium` or `GITLAB_TIER=ultimate`. In HTTP mode, set `--tier=premium`/`--tier=ultimate` to force the Enterprise/Premium catalog, or omit it to let the server auto-detect CE/EE per token+URL entry when GitLab reports edition. The legacy `GITLAB_ENTERPRISE=true` (stdio) and `--enterprise` (HTTP) flags are still honored for back-compat.

## Platform support

Pre-built binaries are available for all major platforms:

| OS      | Architecture          | Binary                                |
| ------- | --------------------- | ------------------------------------- |
| Linux   | amd64                 | `gitlab-mcp-server-linux-amd64`       |
| Linux   | arm64                 | `gitlab-mcp-server-linux-arm64`       |
| macOS   | amd64 (Intel)         | `gitlab-mcp-server-darwin-amd64`      |
| macOS   | arm64 (Apple Silicon) | `gitlab-mcp-server-darwin-arm64`      |
| Windows | amd64                 | `gitlab-mcp-server-windows-amd64.exe` |
| Windows | arm64                 | `gitlab-mcp-server-windows-arm64.exe` |

## MCP client compatibility

Any client supporting the Model Context Protocol stdio transport can use this server. Tested clients:

| Client                     | Transport | Status |
| -------------------------- | --------- | :----: |
| VS Code + GitHub Copilot   | stdio     |   ✅   |
| Claude Desktop             | stdio     |   ✅   |
| Cursor                     | stdio     |   ✅   |
| Claude Code (CLI)          | stdio     |   ✅   |
| Windsurf                   | stdio     |   ✅   |
| JetBrains IDEs             | stdio     |   ✅   |
| Zed                        | stdio     |   ✅   |
| Kiro                       | stdio     |   ✅   |
| Any Streamable HTTP client | HTTP      |   ✅   |

## MCP protocol features

GitLab MCP Server implements the full set of MCP capabilities beyond tools — resources, prompts, completions, elicitation, and progress notifications — so compatible clients get contextual data, prompt templates, and argument autocompletion in addition to tool calls.

| Capability  |                                                                                                                      Supported                                                                                                                       |
| ----------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------: |
| Tools       | ✅ (up to {stats.tools.ultimate_self_managed} self-managed Enterprise / {stats.tools.gitlab_com} GitLab.com + Orbit individual / {stats.meta.base} base, {stats.meta.self_managed_enterprise} self-managed, {stats.meta.gitlab_com} GitLab.com meta) |
| Resources   |                                                                                                                ✅ ({stats.resources})                                                                                                                |
| Prompts     |                                                                                                                 ✅ ({stats.prompts})                                                                                                                 |
| Completions |                                                                                                       ✅ ({stats.completions} argument types)                                                                                                        |
| Elicitation |                                                                                                                          ✅                                                                                                                          |
| Progress    |                                                                                                                          ✅                                                                                                                          |

The {stats.tools.ultimate_self_managed} self-managed and {stats.tools.gitlab_com} GitLab.com figures are the expanded set of distinct tool instances. The {stats.meta.base} base, {stats.meta.self_managed_enterprise} self-managed, and {stats.meta.gitlab_com} GitLab.com counts are meta-tool catalog sizes whose actions expand to that larger individual tool surface.

## Frequently asked questions

### Does GitLab MCP Server work with GitLab Community Edition?

Yes. GitLab MCP Server works with both Community Edition (CE) and Enterprise Edition (EE). On CE it exposes the full base catalog — {stats.meta.base} base meta-tools, {stats.resources} resources, and {stats.prompts} prompts — covering projects, issues, merge requests, pipelines, CI/CD, wikis, releases, users, groups, search, deployments, environments, and packages. Enterprise-only features such as merge trains, DORA metrics, vulnerability management, audit events, and compliance policies require a Premium or Ultimate license and are not available on CE.

### How do I enable Enterprise tools?

Set the licensing tier explicitly or let the server auto-detect it. In stdio mode, set `GITLAB_TIER=premium` or `GITLAB_TIER=ultimate`; in HTTP mode pass `--tier=premium` or `--tier=ultimate`. When the tier is omitted, the server auto-detects the edition from `GET /license` per token+URL entry, falling back to `free`. Forcing the Enterprise/Premium catalog adds 16 enterprise-only meta-tools. The legacy `GITLAB_ENTERPRISE=true` (stdio) and `--enterprise` (HTTP) flags are still honored for back-compat but are deprecated.

### Which operating systems and architectures are supported?

Pre-built binaries are available for Linux, macOS, and Windows on both amd64 and arm64 — six binaries in total. macOS ships separate Intel (amd64) and Apple Silicon (arm64) builds. Each platform is a single static binary with no runtime dependencies.

### Which MCP clients are compatible with GitLab MCP Server?

Any client that supports the Model Context Protocol can use GitLab MCP Server. Tested stdio clients include VS Code + GitHub Copilot, Claude Desktop, Cursor, Claude Code (CLI), Windsurf, JetBrains IDEs, Zed, and Kiro. Any Streamable HTTP client can connect through HTTP mode. The server also supports MCP resources ({stats.resources}), prompts ({stats.prompts}), completions ({stats.completions} argument types), elicitation, and progress notifications.
