---
title: GitLab MCP Server
description: Servidor Model Context Protocol de código abierto para GitLab, con superficies de herramientas dinámicas, meta-herramientas e individuales para asistentes de IA.
datePublished: "2026-04-21"
hero:
  tagline: Conecta tu asistente de IA con GitLab para revisar merge requests, diagnosticar pipelines, gestionar incidencias y redactar releases — en lenguaje natural. Más de 1000 herramientas sobre la API REST + GraphQL completa.
  image:
    dark: ../../../assets/logo-dark.svg
    light: ../../../assets/logo-light.svg
    alt: Logotipo de GitLab MCP Server
  actions:
    - text: Primeros pasos
      link: /gitlab-mcp-server/es/getting-started/
      icon: right-arrow
      variant: primary
    - text: Ver en GitHub
      link: https://github.com/jmrplens/gitlab-mcp-server
      icon: external
      variant: minimal
head:
  - tag: title
    content: GitLab MCP Server — servidor MCP para GitLab y asistentes de IA
faq:
  - q: "¿Qué es GitLab MCP Server?"
    a: "GitLab MCP Server es un servidor [Model Context Protocol](https://modelcontextprotocol.io/), libre y de código abierto, que permite a los asistentes de IA interactuar con GitLab mediante lenguaje natural. Expone las APIs REST v4 y GraphQL de GitLab como herramientas MCP, de modo que un asistente puede revisar merge requests, diagnosticar pipelines, gestionar incidencias y redactar releases por ti. Se distribuye como un único binario de Go y se ejecuta localmente por stdio o de forma remota por HTTP."
  - q: "¿Con qué asistentes de IA funciona?"
    a: "Funciona con cualquier cliente compatible con MCP — incluidos VS Code con GitHub Copilot, Claude Desktop, Cursor y Claude Code, además de cualquier otro cliente que hable el Model Context Protocol por transporte stdio o HTTP. La configuración es un pequeño fragmento JSON por cliente."
  - q: "¿Es gratuito y de código abierto?"
    a: "Sí. GitLab MCP Server es gratuito y de código abierto bajo la licencia MIT, con todo el código fuente en GitHub. No existe una versión de pago del servidor; las funciones de GitLab Premium o Ultimate dependen únicamente de la licencia de tu propia instancia de GitLab, no del servidor MCP."
  - q: "¿Funciona con GitLab autoalojado y Community Edition?"
    a: "Sí. Funciona con GitLab.com y con instancias autoalojadas, y tanto con Community Edition como con Enterprise Edition. Apúntalo a cualquier URL de GitLab con un token de acceso personal. Las herramientas Premium y Ultimate se registran solo cuando la instancia conectada tiene la licencia correspondiente."
---

import {
	Card,
	CardGrid,
	LinkCard,
	Tabs,
	TabItem,
} from "@astrojs/starlight/components";
import FAQ from "../../../components/FAQ.astro";
import stats from "../../../data/stats.json";

**GitLab MCP Server** es un servidor [Model Context Protocol](https://modelcontextprotocol.io/) que permite a los asistentes de IA interactuar con GitLab a través de lenguaje natural. Pide a tu IA que cree issues, revise merge requests, analice pipelines y mucho más — todo sin salir de tu editor.

Expone **más de 1000 operaciones de GitLab** como herramientas MCP — {stats.tools.free} en Community Edition y hasta {stats.tools.gitlab_com} en GitLab.com — en tres modos de herramientas: una superficie **dinámica** de bajo consumo (2 herramientas), **{stats.meta.base} meta-herramientas de dominio**, o una herramienta por operación. Se distribuye como un único binario de Go para Linux, macOS y Windows, y se ejecuta localmente por stdio o como servicio HTTP compartido. Gratuito y de código abierto bajo licencia MIT.

## ¿Qué puede hacer?

GitLab MCP Server permite que tu asistente de IA ejecute tareas de GitLab por ti — consultar, crear y actualizar recursos directamente desde un prompt de chat. En lugar de alternar entre tu editor y la interfaz web de GitLab, simplemente pregunta:

```text
Muéstrame todas las merge requests abiertas en my-project que necesitan revisión
```

```text
¿Por qué falló el pipeline en la rama feature/auth? Resume el error y sugiere una solución
```

```text
Crea un issue titulado "Refactorizar módulo de auth" con etiqueta de prioridad y asígnamelo
```

El servidor traduce estas solicitudes en llamadas a la API de GitLab, las ejecuta y devuelve resultados estructurados que tu asistente de IA puede entender y sobre los que puede actuar.

## Características principales

| Característica                                                                                       | Detalles                                                                                                                                         |
| ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Conjunto dinámico**                                                                                | Modo predeterminado de bajo consumo con 2 herramientas: encontrar acciones canónicas con schemas exactos y ejecutar acciones canónicas           |
| **{stats.meta.base}/{stats.meta.self_managed_enterprise}/{stats.meta.gitlab_com} Meta-herramientas** | Modo opcional por dominio que cubre proyectos, issues, merge requests, pipelines, CI/CD, wikis, releases, Orbit y más                            |
| **{stats.resources} Recursos MCP**                                                                   | Endpoints de datos de solo lectura para proyectos, perfiles de usuario, guías de flujo y el manifiesto `gitlab://tools` adaptado a la superficie |
| **{stats.prompts} Prompts MCP**                                                                      | Plantillas de prompts predefinidas para informes, auditorías, calidad de flujo Git, análisis entre proyectos y flujos de trabajo en equipo       |
| **Modo HTTP multiusuario**                                                                           | Despliega como servidor compartido con aislamiento por token para entornos de equipo                                                             |
| **GitLab CE y EE**                                                                                   | Compatible con Community Edition y Enterprise Edition, incluyendo instancias autoalojadas                                                        |
| **Herramientas Enterprise**                                                                          | Catálogo Premium/Ultimate opcional: 16 meta-herramientas adicionales autoalojadas, más Orbit Knowledge Graph solo en GitLab.com                  |
| **Autoactualización**                                                                                | Mecanismo de autoactualización integrado — ejecuta siempre la última versión                                                                     |
| **Multiplataforma**                                                                                  | Binario único para Linux, macOS y Windows (amd64 y arm64)                                                                                        |

## Ejemplo rápido

**Tú:** "¿Qué merge requests están abiertas en `my-org/backend` y quién está asignado?"

**Asistente de IA** (mediante GitLab MCP Server):

> Se encontraron 3 merge requests abiertas en `my-org/backend`:
>
> | MR   | Título                       | Autor    | Asignado | Pipeline        |
> | ---- | ---------------------------- | -------- | -------- | --------------- |
> | !142 | Add rate limiting middleware | @alice   | @bob     | ✅ exitoso      |
> | !139 | Fix N+1 query in /users      | @charlie | @alice   | ❌ fallido      |
> | !137 | Update OpenAPI spec          | @bob     | —        | ⏳ en ejecución |
>
> **!139** tiene un pipeline fallido — ¿quieres que analice el fallo?

**Tú:** "Sí, comprueba por qué falló !139"

**Asistente de IA:**

> El pipeline #8847 falló en la etapa `test`, job `unit-tests`:
>
> ```text
> FAIL: TestUserService_ListUsers — expected 10 results, got 15
> ```
>
> La corrección del N+1 cambió la consulta para devolver todos los resultados sin aplicar el límite de paginación predeterminado.
> **Solución sugerida:** Añadir `.Limit(defaultPageSize)` a la consulta en `user_repository.go:87`.

## Sin vs con GitLab MCP Server

| Flujo de trabajo            | ❌ Sin                                                     | ✅ Con GitLab MCP Server                                        |
| --------------------------- | ---------------------------------------------------------- | --------------------------------------------------------------- |
| **Revisión de código**      | Cambiar a la UI de GitLab, leer diffs y comentar a mano    | _"Revisa la seguridad del MR !142 y deja comentarios"_          |
| **Depuración de pipelines** | Abrir logs, rastrear jobs fallidos y buscar errores        | _"¿Por qué falló el pipeline de feature/auth?"_                 |
| **Gestión de issues**       | Navegar a Issues, rellenar formularios y asignar etiquetas | _"Crea un bug P1 por la regresión de auth y asígnalo a @alice"_ |
| **Notas de release**        | Leer cada commit desde el último tag y redactar changelog  | _"Genera notas de release para v2.1.0 vs v2.0.0"_               |
| **Resumen de proyecto**     | Abrir varias pestañas: MRs, issues, pipelines, milestones  | _"Dame un informe de estado de my-org/backend"_                 |

## Ejemplos de prompts

Prueba estos con tu asistente de IA una vez conectado GitLab MCP Server:

<Tabs>
	<TabItem label="Proyectos y Código">
		- _"Lista mis proyectos de GitLab"_ - _"Muestra el README del proyecto
		my-app"_ - _"Busca comentarios TODO en el código"_
	</TabItem>
	<TabItem label="Merge Requests">
		- _"Muestra los merge requests abiertos en my-app"_ - _"Resume los cambios
		del MR !42"_ - _"¿Es seguro mergear el MR !15? Revisa problemas de
		seguridad"_
	</TabItem>
	<TabItem label="Issues y Planificación">
		- _"Lista los issues abiertos asignados a mí"_ - _"Crea un bug report
		titulado 'Arreglar página de login' con etiqueta 'bug'"_ - _"¿Cuál es el
		progreso del milestone v2.0?"_
	</TabItem>
	<TabItem label="CI/CD y Pipelines">
		- _"¿Cuál es el estado del último pipeline de my-app?"_ - _"¿Por qué falló
		el último pipeline?"_ - _"Muestra las variables CI del proyecto my-app"_
	</TabItem>
	<TabItem label="Informes y Análisis">
		- _"Genera notas de release de v1.0 a v2.0"_ - _"Dame un resumen de standup
		diario"_ - _"Evalúa el riesgo del merge request !23"_
	</TabItem>
</Tabs>

## Comenzar

<CardGrid>
	<LinkCard
		title="Primeros pasos"
		href="/gitlab-mcp-server/es/getting-started/"
		description="Instala el binario y conéctalo a tu primer cliente de IA en minutos"
	/>
	<LinkCard
		title="Asistente de configuración"
		href="/gitlab-mcp-server/es/setup-wizard/"
		description="Configura clientes stdio con el asistente Web, TUI o CLI"
	/>
	<LinkCard
		title="Arquitectura"
		href="/gitlab-mcp-server/es/architecture/"
		description="Comprende cómo el servidor conecta los asistentes de IA con GitLab"
	/>
	<LinkCard
		title="Configuración"
		href="/gitlab-mcp-server/es/configuration/"
		description="Variables de entorno, configuraciones de clientes y opciones de despliegue"
	/>
	<LinkCard
		title="Referencia de herramientas"
		href="/gitlab-mcp-server/es/tools/overview/"
		description="Explora todas las herramientas MCP disponibles por dominio"
	/>
	<LinkCard
		title="Conjunto dinámico"
		href="/gitlab-mcp-server/es/tools/dynamic-tools/"
		description="Usa el modo de bajo consumo para buscar y ejecutar"
	/>
</CardGrid>

## Cómo funciona

```mermaid
graph LR
    A[Tú] -->|lenguaje natural| B[Cliente IA]
    B -->|protocolo MCP| C[GitLab MCP Server]
    C -->|REST v4 + GraphQL| D[Instancia GitLab]
    D -->|respuesta JSON| C
    C -->|resultado estructurado| B
    B -->|respuesta formateada| A
```

El servidor actúa como puente: tu cliente de IA envía llamadas a herramientas a través del protocolo MCP, el servidor las traduce en peticiones a la API REST v4 o GraphQL de GitLab y devuelve los resultados tanto en JSON estructurado (para la IA) como en Markdown formateado (para ti).

## Clientes de IA compatibles

GitLab MCP Server funciona con cualquier cliente compatible con MCP:

- **VS Code + GitHub Copilot** — mediante configuración `mcp.json`
- **Claude Desktop** — mediante `claude_desktop_config.json`
- **Cursor** — mediante `.cursor/mcp.json`
- **Claude Code** — mediante `claude code mcp add`
- **Cualquier cliente MCP** — transporte stdio o HTTP

<FAQ />
