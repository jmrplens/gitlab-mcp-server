---
title: Casos de uso
description: Flujos de trabajo comunes de GitLab MCP Server — revisión de código, depuración CI/CD, gestión de issues, releases, informes y seguridad, en lenguaje natural.
datePublished: "2026-04-21"
faq:
  - q: "¿Qué puedo hacer con GitLab MCP Server?"
    a: 'GitLab MCP Server permite a un asistente de IA manejar GitLab mediante lenguaje natural en ocho flujos de trabajo comunes: revisión de código, automatización CI/CD, gestión de issues, gestión de releases, gestión de equipo, búsqueda de código, informes y analítica, y revisión de seguridad. En lugar de cambiar a la interfaz web de GitLab, describes el resultado — por ejemplo, _"Resume los cambios del MR !42 y comprueba problemas de seguridad"_ — y el asistente llama a las herramientas de GitLab correspondientes, las ejecuta y devuelve el resultado. Cada flujo de trabajo se corresponde con operaciones REST y GraphQL reales de GitLab expuestas como herramientas MCP.'
  - q: "¿GitLab MCP Server puede revisar merge requests y dejar comentarios?"
    a: 'Sí. El flujo de revisión de código usa las herramientas `gitlab_merge_request`, `gitlab_mr_changes` y `gitlab_mr_discussion` para que un asistente pueda listar MRs pendientes de revisión, resumir diffs, ejecutar un análisis de seguridad, publicar comentarios de revisión y aprobar. Una conversación típica es: _"Muestra los MRs abiertos en my-app que necesitan revisión"_, luego _"Resume los cambios del MR !42"_, _"Comprueba el MR !42 por problemas de seguridad"_, _"Deja un comentario sugiriendo añadir validación de entrada"_ y _"Aprueba el MR !42"_. Cada paso es una llamada de herramienta separada contra la API de GitLab.'
  - q: "¿Cómo ayuda GitLab MCP Server a depurar pipelines CI/CD?"
    a: 'El flujo CI/CD usa `gitlab_pipeline`, `gitlab_job` y `gitlab_ci_variable` para inspeccionar y recuperar pipelines con fallos sin salir del editor. Puedes pedir el estado del último pipeline, listar los jobs fallidos de un pipeline concreto, obtener los logs de un job para encontrar el error, comprobar las variables CI del proyecto y reintentar los jobs fallidos. Por ejemplo, _"¿Por qué falló el último pipeline en la rama feature/auth?"_ devuelve la etapa y el job con fallos para que el asistente explique el error y sugiera una solución.'
  - q: "¿GitLab MCP Server puede generar informes y notas de release?"
    a: 'Sí. El flujo de informes se apoya en prompts MCP predefinidos como standup, risk-assessment, workload y milestone-report para producir resúmenes de standup diarios, evaluaciones de riesgo, informes de progreso de hitos y análisis de carga de trabajo del equipo. La gestión de releases combina `gitlab_release`, `gitlab_tag` y `gitlab_release_link` para crear releases, gestionar tags y subir assets — por ejemplo, _"Genera notas de release comparando v1.0 con v2.0"_ ensambla un changelog a partir de los commits entre dos refs.'
---

import { Card, CardGrid } from "@astrojs/starlight/components";
import FAQ from "../../../components/FAQ.astro";

Con GitLab MCP Server puedes revisar merge requests, depurar pipelines CI/CD con fallos, crear y triar issues, publicar releases y changelogs, gestionar el acceso del equipo, buscar en código e issues, generar informes de standup y de hitos, y ejecutar revisiones de seguridad — todo desde tu asistente de IA en lenguaje natural, sin abrir la interfaz web de GitLab. Esto se corresponde con ocho flujos de trabajo comunes — revisión de código, automatización CI/CD, gestión de issues, gestión de releases, gestión de equipo, búsqueda de código, informes y analítica, y revisión de seguridad — cada uno respaldado por operaciones REST y GraphQL reales de GitLab expuestas como herramientas MCP. Las tarjetas siguientes agrupan esas capacidades por flujo de trabajo en lugar de por dominio de API.

## Flujos de trabajo

<CardGrid>
 <Card title="🔀 Revisión de código" icon="pencil">
  Revisa merge requests, analiza cambios, comprueba problemas de seguridad y deja comentarios — todo mediante lenguaje natural.

     **Herramientas clave:** `gitlab_merge_request`, `gitlab_mr_changes`, `gitlab_mr_discussion`

     **Prueba:** _"Resume los cambios del MR !42 y comprueba problemas de seguridad"_
    </Card>

    <Card title="🔄 Automatización CI/CD" icon="setting">
     Monitoriza pipelines, depura fallos, gestiona variables CI y revisa programaciones de pipelines sin salir de tu editor.

     **Herramientas clave:** `gitlab_pipeline`, `gitlab_job`, `gitlab_ci_variable`

     **Prueba:** _"¿Por qué falló el último pipeline en la rama feature/auth?"_
    </Card>

    <Card title="📋 Gestión de issues" icon="list-format">
     Crea, actualiza y haz seguimiento de issues. Gestiona etiquetas, hitos y asignaciones mediante conversación.

     **Herramientas clave:** `gitlab_issue`, `gitlab_label`, `gitlab_milestone`

     **Prueba:** _"Crea un bug report titulado 'Arreglar página de login' con etiqueta 'bug' y asignar a @alice"_
    </Card>

    <Card title="📦 Gestión de releases" icon="rocket">
     Crea releases, genera changelogs, gestiona tags y sube assets de release.

     **Herramientas clave:** `gitlab_release`, `gitlab_tag`, `gitlab_release_link`

     **Prueba:** _"Genera notas de release comparando v1.0 con v2.0"_
    </Card>

    <Card title="👥 Gestión de equipo" icon="seti:ignored-vscode">
     Gestiona miembros de proyecto, membresías de grupo y niveles de acceso.

     **Herramientas clave:** `gitlab_user`, `gitlab_group`, `gitlab_member`

     **Prueba:** _"Añade a @bob como developer en el proyecto my-app"_
    </Card>

    <Card title="🔍 Búsqueda de código" icon="magnifier">
     Busca en código, issues, merge requests y wikis. Explora el árbol del repositorio y compara ramas.

     **Herramientas clave:** `gitlab_search`, `gitlab_repository`, `gitlab_file`

     **Prueba:** _"Busca todos los comentarios TODO en el proyecto my-app"_
    </Card>

    <Card title="📊 Informes y analítica" icon="open-book">
     Genera resúmenes de standup, evaluaciones de riesgo, informes de hitos y análisis de carga de trabajo usando prompts con IA.

     **Prompts clave:** standup, risk-assessment, workload, milestone-report

     **Prueba:** _"Dame un resumen de standup diario del proyecto my-app"_
    </Card>

    <Card title="🛡️ Revisión de seguridad" icon="approve-check">
     Audita merge requests en busca de problemas de seguridad, revisa vulnerabilidades y analiza configuraciones CI.

     **Herramientas clave:** `gitlab_vulnerability`, `gitlab_audit_event`

     **Prueba:** _"Revisa el MR !42 en busca de vulnerabilidades de seguridad"_
    </Card>

</CardGrid>

## Ejemplos de flujos de trabajo

### Flujo de revisión de código

1. _"Muestra los MRs abiertos en my-app que necesitan revisión"_ — lista MRs esperando revisión
2. _"Resume los cambios del MR !42"_ — obtiene un resumen del diff
3. _"Comprueba el MR !42 por problemas de seguridad"_ — ejecuta análisis de seguridad
4. _"Deja un comentario en el MR !42 sugiriendo añadir validación de entrada"_ — añade feedback de revisión
5. _"Aprueba el MR !42"_ — aprueba el merge request

### Flujo de depuración CI/CD

1. _"¿Cuál es el estado del pipeline de my-app en main?"_ — comprueba el último pipeline
2. _"Muestra los jobs fallidos del pipeline #8847"_ — identifica las etapas con fallos
3. _"Dame los logs del job unit-tests del pipeline #8847"_ — obtiene detalles del error
4. _"¿Qué variables CI están configuradas en el proyecto my-app?"_ — comprueba la configuración
5. _"Reintenta los jobs fallidos del pipeline #8847"_ — re-ejecuta los fallos

### Flujo de planificación de sprint

1. _"Muestra el hito v3.0 del proyecto my-app"_ — revisa el alcance del hito
2. _"Lista los issues abiertos del hito v3.0"_ — ve el trabajo pendiente
3. _"Crea un issue para 'Implementar rate limiting' en el hito v3.0"_ — añade tareas faltantes
4. _"Asigna el issue #234 a @alice"_ — distribuye el trabajo
5. _"Genera un informe de progreso del hito v3.0"_ — crea resumen de estado

<FAQ />

## Referencia de la fuente

:::note[Documentación para desarrolladores]
Esta página agrupa las capacidades por flujo de trabajo. Para transcripciones de comandos listas para copiar y recetas de extremo a extremo, consulta [docs/guides/examples](https://github.com/jmrplens/gitlab-mcp-server/blob/main/docs/guides/examples/usage-examples.md).
:::
