// Package ce holds the shapes the static gate must accept from the package
// that runs only on an unlicensed instance: it is a declared placement like
// common and ee, so a Free action named here is clean.
package ce
